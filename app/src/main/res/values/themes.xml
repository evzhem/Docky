<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--
      Тема Activity Taskbar: полупрозрачное плавающее окно.
      - windowIsTranslucent = true: за окном виден рабочий стол/обои;
      - windowBackground = transparent: фон рисует Compose (скругления и тень);
      - backgroundDimEnabled = false: без затемнения позади окна.
    -->
    <style name="Theme.Taskbar" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Activity</item>
    </style>

</resources>
